import { StyledForm, StyledDiv, HeroTitle } from './Hero.styles'

const Hero = () => (
  <StyledForm>
    <StyledDiv className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </StyledDiv>
  </StyledForm>
)

export default Hero
